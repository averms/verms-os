# Xorg because only wayland is installed by default.
gnome-session-xsession
xorg-x11-drivers
xorg-x11-xinit

# Packages I want on the host.
code
distrobox
gnome-shell-extension-appindicator
gnome-shell-extension-auto-move-windows
google-noto-fonts-all-vf
kitty
libva-utils
man-pages-posix
smartmontools
syncthing
tailscale
xxd
steam

# Deps of steam 
https://github.com/averms/verms-os/releases/download/fix-rpmfusion-packages/mesa-va-drivers-freeworld-25.1.9-1.fc42.i686.rpm
https://github.com/averms/verms-os/releases/download/fix-rpmfusion-packages/mesa-vulkan-drivers-freeworld-25.1.9-1.fc42.i686.rpm

# CUDA, depends on already having installed kmod-nvidia.
xorg-x11-drv-nvidia-cuda

# Firefox hardware decoding
libva-nvidia-driver

# Keep kernel-modules-extra.
kernel-modules-extra-matched

# gcc is needed because of Homebrew bug. See
# https://github.com/orgs/Homebrew/discussions/5627. libstdc++-devel is also needed
# because clang from Homebrew uses it. So just install the full gcc-c++ package.
gcc-c++

# Needed because Homebrew version is deficient in some way.
netcat
strace
